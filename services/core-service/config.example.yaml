SERVER_PORT: "8080"

# Database
DB_HOST: "localhost"
DB_PORT: "5432"
DB_USER: "your_db_user"
DB_PASSWORD: "your_db_password"
DB_NAME: "plant_disease_db"
DB_SSLMODE: "disable"

# Redis (used for caching / future session management)
REDIS_HOST: "localhost"
REDIS_PORT: "6379"
REDIS_PASSWORD: ""

# JWT — replace with a strong random secret (min 32 characters)
JWT_SECRET: "replace_with_a_strong_random_secret_min_32_chars"
JWT_EXPIRY_MINUTES: 15
