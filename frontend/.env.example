# Copy this file to .env for local development
# For Vercel: set this in Project Settings → Environment Variables

# URL of your deployed FastAPI backend (no trailing slash)
# Leave empty during local development — Vite's proxy handles it
VITE_API_URL=
